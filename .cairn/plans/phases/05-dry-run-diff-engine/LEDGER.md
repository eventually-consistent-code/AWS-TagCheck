# Phase 5: dry run diff engine — Ledger

<!-- append-only; one line per verified task; server appends, never rewrites -->

- [x] 40 — Read-only --dry-run-diff: AWS-free rule-ID set-diff core (normalization + would-remove drop case), read-only S3 GET/list fetch (no-mutating-call pinned), CLI orchestration + rule-by-rule render with drop-warning. S3-only, content-based freshness. — commits 8a2a06e..d62ba2f — 40 closed 2026-08-04
