# pycodestyle config (and historical pep8 section name for tooling).
[pycodestyle]
max-line-length = 120
exclude = .venv,build,dist,*.egg-info

[pep8]
max-line-length = 120
exclude = .venv,build,dist,*.egg-info
