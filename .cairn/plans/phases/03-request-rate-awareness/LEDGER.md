# Phase 3: request rate awareness — Ledger

<!-- append-only; one line per verified task; server appends, never rewrites -->

- [x] 38 — READ/WRITE op classification + per-prefix rate estimation (min-window guard) + persisted request_rates + top-precedence prefix-fanout rec (240 tests, lint green) — commits 0fb20e0..f3ca0c2 — 38 closed 2026-07-31
